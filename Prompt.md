由于后期还需要扩展更多的功能, 同时也是为便于后期的维护, 所以我希望你严格按照下面要求来实现
> 写代码前请严格遵守以下要求，杜绝屎山：  
> 1. 所有代码模块化、职责单一，禁止大杂烩函数和超长文件；逻辑稍复杂就拆分函数或类。  
> 2. 严格分层：UI、业务逻辑、数据访问、配置、工具等必须分层，不得交叉污染或环状依赖。  
> 3. 禁止隐式共享状态和随意全局变量；必要的共享状态必须集中管理，通过明确接口访问。  
> 4. 函数输入输出必须清晰，不依赖隐藏全局状态，不悄悄修改和自身职责无关的外部对象。  
> 5. 命名必须表达清楚意图，禁止 data1/tmp/a1 这类无意义名字（短生命周期局部变量除外）。  
> 6. 业务核心逻辑必须可单元测试，不依赖 UI、文件或网络；外部交互通过适配层封装。  
> 7. 禁止代码复制粘贴、到处 try/except 吃异常、以及混杂多层抽象（UI + 业务 + IO 混在一个函数里）。  
> 8. 不要增加任何我没明确要求的功能。如果有更合理的结构或设计，请先用文字简短说明，再按你认为最佳的可维护结构实现，但范围不能超出我的需求。